# Copy to .env locally. On Railway, set these in the service Variables UI.

# Required: admin JWT signing (minimum 16 characters)
JWT_SECRET=

# Optional: canonical public URL for Open Graph / metadata (e.g. https://your-service.up.railway.app)
NEXT_PUBLIC_SITE_URL=
