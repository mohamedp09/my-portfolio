# Template only — do NOT put real secrets here; this file is committed to Git.
# Copy to `.env` (ignored by git) for local dev, or set variables in Railway/Vercel dashboard.

# Required: random string, min 16 characters (generate a new one for production)
JWT_SECRET=

ADMIN_USERNAME=
ADMIN_PASSWORD=

# Optional (production default in code: https://mohamedbuilds.org)
NEXT_PUBLIC_SITE_URL=
